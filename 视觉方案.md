# 视觉方案

本文只描述 H、E 两题中与摄像头、图像处理、坐标标定及视觉—控制通信有关的部分。方案默认使用现有 Jetson、RealSense D435 RGB 相机、OpenCV、YOLO/TensorRT 和串口框架；最终选题确定后，只保留对应章节。

## 一、H 题：车载平衡滚球运动控制系统

### 1. 赛题对视觉的硬性要求

- 摆杆长 25 cm，由 4 分 PPR 管制成，槽内钢球直径约 1 cm。
- 凹槽边沿有 0.1 cm 间距的刻度线；钢球位置检测必须使用摄像头。
- 摄像头和图传发送模块必须固定在车上，画面要覆盖整根摆杆，能够看清钢球完整运动轨迹并判定位置。
- 接收端位于赛道外，要稳定实时显示，并完整保存每次测试视频、支持回放。
- 控制目标包括中心 O、+5 cm、-5 cm及任意指定位置，主要位置误差绝对值不超过 1 cm。

因此视觉模块不能只做“有无钢球”检测，必须连续输出钢球相对 O 点的带符号一维位置，同时独立完成实时图传和录像。

### 2. 坐标定义与输出

建立摆杆一维坐标系：

- 摆杆中心 O 为 `0 cm`；
- 从铰链端指向执行机构端定义为正方向；
- 另一方向为负方向；
- 管道几何端点为 `[-12.5 cm, +12.5 cm]`；直径1 cm钢球的球心
  理论可达量程约为 `[-12.0 cm, +12.0 cm]`。

视觉输出至少包含：

| 字段 | 含义 | 建议单位 |
|---|---|---|
| `position` | 钢球相对 O 点的带符号位置 | 0.01 cm |
| `velocity` | 钢球沿摆杆方向的速度 | 0.01 cm/s |
| `confidence` | 本帧检测置信度 | 0～255 |
| `valid` | 位置是否可信 | 0/1 |
| `timestamp` | 采样时刻 | ms |

控制器的误差为 `e = target_position - position`。视觉正方向必须与控制器正方向在联调时确认一次，严禁在两端各自猜测符号。

### 3. 相机与安装

- 相机固定在车体上方，光轴尽量垂直摆杆，整根 25 cm 摆杆至少占画面宽度的 70%。
- 推荐 RGB/深度画面为 640×480、60 fps；实际视觉处理帧率由 YOLO 推理速度决定，裁剪摆杆 ROI 可进一步降低延迟。
- 相机、补光件和支架必须随车固定，线缆做应力释放；急转弯和摆杆动作时画面不得明显抖动。
- 建议增加柔和、低功率、直流常亮的白色补光，避免钢球高光随室内照明变化；补光不能形成频闪。
- 锁定曝光、增益、白平衡和焦距，比赛前完成预热后再标定。
- 若 D435 体积或重量影响小车，可改用普通广角 USB RGB 相机；本题只需要 RGB，不依赖深度。

### 4. 标定方法

推荐采用“每帧识别摆杆轴线 + 一次性物理长度标定”，避免车体振动造成固定像素坐标失效。

1. 优先用现有刻度的两端或 PPR 管外轮廓定位管道几何端点 `-12.5 cm` 和 `+12.5 cm`。钢球球心受0.5 cm半径限制，只能到约 `-12.0 cm` 和 `+12.0 cm`。只有在赛前确认规则允许时，才在凹槽外增加两个不遮挡刻度、也不妨碍钢球运动的定位标记，不能贴进凹槽。
2. 检测两标记中心 `P-`、`P+`，得到摆杆单位方向  
   `u = (P+ - P-) / |P+ - P-|`。
3. 中心点像素坐标为 `PO = (P- + P+) / 2`。
4. 钢球像素中心为 `Pb`，则带符号位置为  
   `x = dot(Pb - PO, u) × 25 / |P+ - P-| cm`。
5. 用凹槽边沿的 0.1 cm 刻度做离线校验，不建议逐帧识别全部刻度。

如果透视畸变明显，应以多个已知刻度点拟合一维二次映射，或对摆杆平面做单应变换后再测距。标定后用 `-10、-5、0、+5、+10 cm` 五点检查，要求静态最大误差不超过 0.3 cm，给控制误差和动态模糊留出余量。

### 5. 钢球检测

采用“传统视觉主通路 + YOLO 兜底”：

1. 用两端标记确定摆杆 ROI，并把 ROI 旋转成水平图像。
2. 在凹槽区域内使用灰度、高光、圆度和尺寸约束提取钢球候选。
3. 通过轮廓最小外接圆或圆拟合求亚像素中心。
4. 候选必须位于凹槽带状区域内，直径应接近标定后的 1 cm 像素尺寸。
5. 当传统方法因反光、阴影失效时，调用现有钢球 YOLO/TensorRT 模型；取检测框中心投影到摆杆轴线。
6. 若两条通路同时有效且差值小于 0.5 cm，优先使用圆拟合中心；差值过大则降低置信度，不把异常值送入控制。

传统视觉延迟小、中心精度高，YOLO负责提高复杂光照下的检出率。现有 `yolo_camera.py` 已能使用 RealSense RGB 和 TensorRT，但还需要增加 ROI 标定、厘米坐标换算、连续输出与录像功能。

### 6. 跟踪、滤波和失效处理

- 使用常加速度 Kalman 滤波器，状态为 `[位置, 速度, 加速度]`；控制只使用位置和速度，加速度用于标定和实验性观测。
- 钢珠YOLO实测约15～20 FPS。控制器只在新鲜且被接受的视觉帧到达时更新一次，帧间保持上一倾角；25 Hz循环只做超时监督，不能把50 Hz串口重发频率写成控制决策频率。
- 基于最大物理速度设置帧间跳变门限；超限检测作为离群值丢弃。
- 超过0.25 s没有新鲜有效测量时控制器立即输出0°并清除连续稳定计时；旧帧、重复或倒退采集时间不得给控制续命。不得把 `0 cm` 当成“未检测到”。
- 每帧记录原始位置、滤波位置、置信度、控制目标和丢帧状态，便于赛后定位问题。

### 7. 与控制器通信

位置闭环在Jetson本机运行，控制器内部和终端保留最终目标倾角，但向单片机
发送由 `h_mm=250*tan(theta)` 得到的电机端升降距离。线路负载固定为
8个原始字节，包含帧头和帧尾：

```text
[0x92, sign, integer, hundredths, 0, 0, 0, 0x29]
```

- `sign=0x00/0x01` 分别表示正/负；
- `integer` 和 `hundredths` 分别表示升降毫米数的整数部分和两位小数；
- 位移严格限制为 `-99.99～+99.99 mm`，对应倾角约
  `-21.80°～+21.80°`；
- 正位移为电机抬高，负位移为电机下降，0°对应电机零位；
- 串口长期打开，由独立线程以50 Hz重发最新位移，正常退出前最后发送零位。

9600 baud、8N1下50 Hz八字节约占4000 bit/s。单片机端搜索0x92帧头，收满
8字节后验证0x29帧尾，错误时重新同步到下一个帧头。

### 8. 图传、显示和录像

图传与控制检测共用摄像头画面，但两条链路解耦：

- 控制链路始终处理本地原始帧，不能等待无线网络或显示端。
- 显示画面叠加中心 O、正方向、当前位置、目标位置、误差、FPS 和 `valid` 状态。
- 每次按键开始测试时自动创建带时间戳的视频文件，停止条件触发后安全关闭文件；保存原始或带叠加的完整视频。
- 无线图传可用低延迟 H.264 推流；接收端笔记本/PAD位于环形赛道外，同时显示并保存。
- 接收模块及其连接的笔记本/PAD按赛题要求随作品一起封存，正式方案不能依赖赛后取走的个人设备或云端服务。
- 网络中断只能影响远端显示，不能阻塞位置输出和摆杆控制；恢复后自动继续显示。
- 正式测试前必须实际回放一次文件，确认时长、帧率和画面均正常。

### 9. H 题验收指标

| 项目 | 内部目标 |
|---|---|
| 五点静态最大误差 | ≤0.3 cm |
| 动态位置均方根误差 | ≤0.5 cm |
| 输出频率 | ≥25 Hz |
| 端到端视觉延迟 | ≤80 ms |
| 连续运行丢检率 | <1% |
| 画面覆盖 | 完整包含 25 cm 摆杆和钢球轨迹 |
| 录像 | 每次测试自动保存且可回放 |

测试要覆盖静止、快速往返、车辆直线加减速、半圆转弯、不同室内光照和图传断开等工况。

---

## 二、E 题：拼图装置

### 1. 赛题对视觉的硬性要求

- 纵向 A4 纸上半区随机、不重叠摆放碎片，下半区完成目标矩形。
- 摄像头先被遮挡；碎片摆好后，一键启动并移除遮挡，装置必须全自动完成，中途不得人工干预。
- 自备碎片为 4 片同色纯色碎片。
- 现场未知碎片不超过 4 片，每片不超过 5 条边，每边不少于 2 cm，每片至少一条边属于目标矩形外边。
- 现场碎片为薄铁片覆纸：一组为纯白色，一组为白底扑克牌图案。
- 最终矩形尺寸在 9×5 cm 到 12×9 cm 之间；位置正确、不重叠，相邻对应顶点距离不大于 2 cm。
- 正常室内照明下工作，不允许提出特殊照明条件。

E 题视觉输出不能只有“碎片中心和角度”。完整任务是：识别所有碎片的轮廓与当前位姿，求出每片在目标矩形中的目标位姿，再向机械臂下发抓取位姿和放置位姿。

### 2. 总体流程

```text
等待一键启动
→ 检查遮挡已移除、画面稳定
→ A4 纸定位与俯视矫正
→ 分割上半区所有碎片
→ 提取轮廓、顶点、纹理和当前位姿
→ 求解目标矩形拼接关系
→ 规划抓取顺序和放置位姿
→ 每放一片后重新拍照校正
→ 最终检查矩形、重叠和接缝
→ 声光提示完成并停止计时
```

### 3. 场地设计与相机标定

- 选用无反光的深色纯色 A4 纸，例如哑光蓝或绿色。这样自备彩色碎片、现场白色碎片和扑克牌碎片都容易与背景分离。
- 上下区域分界线使用与背景对比明显、宽度不超过 5 mm 的实线。
- 顶视相机固定在 A4 纸正上方，完整包含纸张四边及机械臂工作区；尽量使用窄视场并远离纸面，降低透视和广角畸变。
- 先用棋盘格标定相机内参并去畸变。
- 每帧检测 A4 四角，计算单应矩阵，将图像矫正为以毫米为单位的俯视平面。A4 尺寸固定为 210×297 mm，可直接建立纸面坐标系。
- 建议纸面坐标原点设为 A4 左上角，x 向右、y 向下；机器人坐标通过 4～9 个标定点拟合二维仿射或单应变换。
- 标定后在纸面多点放置标记，视觉到机器人坐标的平均误差目标小于 1 mm、最大误差小于 2 mm。

不建议把 RealSense 深度作为主定位依据：碎片厚度小于 2 mm，彩色轮廓比深度边缘稳定。D435只使用 RGB 即可。

### 4. 碎片分割与轮廓提取

#### 4.1 自备纯色碎片

在 HSV 或 Lab 空间按颜色分割，结合开闭运算去除噪声。颜色阈值在赛前保存，启动后可用纸张背景区域自动修正亮度。

#### 4.2 现场白色碎片和扑克牌碎片

利用深色 A4 背景，按亮度和色差提取碎片的白色纸面。扑克牌内部的红黑花纹不能被误当成孔洞，应先对外轮廓填充，再提取整体轮廓。

#### 4.3 通用轮廓处理

1. 排除 A4 边缘、分界线、机械臂和面积过小的连通域。
2. 对每个碎片轮廓做亚像素边缘优化。
3. 使用 `approxPolyDP` 得到 3～5 个主顶点；容差按毫米设置，不能固定为像素。
4. 对每条边保存长度、方向、端点、外侧颜色/纹理描述子。
5. 面积、边长和碎片数必须满足赛题范围，否则不启动机械臂并给出故障指示。

### 5. 当前抓取位姿

对每片碎片输出：

- `piece_id`：稳定的碎片编号；
- `pick_x, pick_y`：纸面/机器人坐标下的抓取点；
- `pick_angle`：碎片当前角度；
- `polygon`：按统一方向排列的顶点；
- `confidence`：分割和位姿置信度。

对于规则碎片，几何质心可作初始抓取点；更稳妥的方法是在轮廓内部计算距离变换，选择“距所有边最远点”作为抓取点，避免磁吸头或吸盘压到边缘。`minAreaRect` 的角度存在 90°跳变，不能直接当作机械臂角度；应使用最长边或指定顶点建立稳定朝向，并把角度归一化到机械腕可达范围。

现场碎片含铁片，可优先使用电磁铁抓取；视觉仍应确认抓取后原位置的碎片已消失，否则重试一次。

### 6. 目标矩形求解

#### 6.1 自备 4 片碎片

自备碎片的几何形状固定，可在赛前保存每片模板及其在目标矩形中的目标位姿。运行时用轮廓的边长序列和角度序列完成模板匹配，只需求当前刚体变换，再查表获得放置位姿。这一路径速度快、可靠性最高。

#### 6.2 现场纯白未知碎片

对不超过 4 片的多边形做几何约束回溯搜索：

1. 枚举每片可作为矩形上、下、左、右边界的候选外边，并由“该边转到对应水平/竖直方向”直接算出连续旋转角。每片至少有一条外边，可显著缩小搜索空间。
2. 两条内部接缝只有在长度接近、方向相反、端点可重合时才能配对。
3. 将候选碎片通过接缝对齐，组成无重叠的多边形并计算并集。
4. 候选并集必须接近矩形，宽高落在题目范围内，面积应接近所有碎片面积之和。
5. 以外轮廓矩形度、接缝端点误差、重叠面积和空洞面积构造代价函数，选总代价最低的解。

由于最多只有 4 片，可以穷举“碎片顺序 × 候选边 × 边界方向”，无需训练神经网络。变换只允许平移和旋转、不允许镜像翻转。若最佳解和次优解代价接近，应先用更严格的亚像素轮廓重新计算，而不是盲目抓取。

#### 6.3 现场扑克牌图案碎片

先用上述几何约束产生少量候选拼法，再用纹理连续性消除歧义：

- 保存每条候选接缝内侧一定宽度的 RGB/灰度条带；
- 将两片按候选目标位姿变换后，对接缝两侧做颜色差、梯度方向差和 ORB 特征匹配；
- 红色和黑色花纹分别计算连续性，避免灰度化后混淆；
- 最终代价为“几何代价 + 纹理断裂代价”。

不要只做扑克牌数字或花色分类；评分看的是相邻花纹是否正确，接缝纹理连续性比牌面语义识别更直接。

### 7. 放置坐标和机械臂接口

目标矩形应放在 A4 下半区中央，并留出机械臂末端和相机观察余量。求解器为每片生成目标多边形，由当前多边形到目标多边形求二维刚体变换：

```text
target_point = R(theta) × current_point + t
```

由此得到：

- 抓取中心 `(pick_x, pick_y)`；
- 放置中心 `(place_x, place_y)`；
- 机械腕旋转量 `delta_angle = place_angle - pick_angle`。

放置顺序优先选目标矩形角部的碎片，再沿已有边界扩展，使每次放置都有一至两条可对齐基准边。下发机械臂前同时做：

- 工作空间检查；
- 末端角度范围检查；
- 路径是否经过未抓取碎片的碰撞检查；
- 抓取点到碎片边缘的安全距离检查。

视觉与机械臂之间建议使用带序号和校验的结构化消息，至少包含 `piece_id、pick_x、pick_y、place_x、place_y、delta_angle、confidence`。若继续使用 8 字节 UART，应拆成多种消息并带同一任务序号，不能把所有坐标强塞进单帧后失去精度。

### 8. 闭环校正与失败恢复

- 每抓起一片后拍照，确认上半区对应轮廓消失；未抓起则回原位并小范围搜索重抓。
- 每放下一片后重新检测其轮廓，计算实际目标位姿误差；超限时进行一次二次校正。
- 已放碎片加入禁入区，后续轨迹不得碰撞。
- 最后一片完成后检测所有碎片并集，检查外接矩形尺寸、重叠、空洞、对应顶点间距和扑克牌纹理连续性。
- 完成条件满足后才发声光指示；算法异常、相机遮挡未移除、碎片数错误或求解无唯一解时给出不同故障码。
- 全流程设置 110 s 软件超时，给赛题 120 s 上限留出动作收尾时间。

### 9. E 题验收指标

| 项目 | 内部目标 |
|---|---|
| A4 四角重投影误差 | ≤0.5 mm |
| 视觉—机械臂平面标定最大误差 | ≤2 mm |
| 碎片中心重复定位误差 | ≤1 mm |
| 碎片角度重复定位误差 | ≤1° |
| 相邻顶点间距 | 内部目标 ≤5 mm，远严于赛题 2 cm |
| 碎片漏检/误检 | 连续 20 次随机摆放均为 0 |
| 完成时间 | ≤110 s |

测试集至少包括：不同室内灯光、碎片靠近分界线、碎片相互距离很小、任意旋转、扑克牌红黑花纹跨接缝、机械臂遮挡、首次抓取失败和相机短时模糊。

---

## 三、实现优先级

### H 题

1. 完成两端点标定和钢球厘米坐标输出。
2. 打通串口，闭环控制中心 O 和 ±5 cm。
3. 做动态滤波、丢检保护和延迟测试。
4. 完成远端实时图传、自动录像和回放。
5. 最后再调 YOLO兜底及画面叠加。

### E 题

1. 固定相机，完成 A4 单应标定和机器人手眼标定。
2. 打通自备碎片的分割、模板匹配、抓取和固定目标位姿放置。
3. 加入每片放置后的闭环校正。
4. 实现现场白色未知碎片的几何拼接搜索。
5. 最后增加扑克牌接缝纹理评分和异常恢复。

H 题的视觉工程风险较低，核心是稳定、低延迟的一维测量；E 题的主要风险在未知碎片拼接求解和机械臂标定，必须尽早用真实尺寸碎片做端到端闭环测试。

---

## 四、实现方案

### H 题
1、建立一个H文件夹放H题相关内容。摄像头坐标系和摄像头底座坐标系的相对关系先定义完全重合，后续我会修改。之前做过钢珠识别的yolo，你需要识别钢珠在画面中的位置并且返回该位置的深度信息。这个深度信息是在摄像头坐标系上面的，然后需要转化到摄像头底座坐标系下面。

### E 题
识别0-3多边形，根据特征拟合。特征主要是各个角度和角度之间顺序关系，以及边长比例关系之类的，就是作多边形的拟合。然后你需要对于每个多边形事先定义多边形中心以及多边形的平面坐标系（即这个中心为坐标原点，每个多边形顶点在坐标系中的位置相同），识别的时候你需要提取多边形中心，这就是我要发给机械臂的坐标点。然后识别这个坐标系在摄像头底座坐标系的xy坐标系中的旋转角，然后读取realsense的深度得到这个多边形中心在摄像头底座坐标系中的位置坐标。位置和旋转都有了，就能得到这个多边形自身坐标系在摄像头底座坐标系中的位置姿态（完整的旋转平移矩阵）。摄像头坐标系和摄像头底座坐标系的相对关系你先定义重合，后续我会修改。

## 五、报告记录

### 2026-07-29：E 题多边形计算工具

- 新建 `E/polygon_solver.py`：支持输入多边形边数及逐边“边长、角度”，用一个 `x` 表示未知边长或未知角度；支持绝对方向角与内角两种模式，并用闭合残差校验结果。
- 为命令行求解器增加 Matplotlib 绘图，标注顶点、边号、边长和角度；未知量所在边标红，数据不相容时用虚线显示闭合残差。
- 新建 `E/polygon_gui.py` 和 `E/start_polygon_gui.sh`：提供 Tkinter 简易界面，可动态生成 3～20 条边的输入框、计算、嵌入式绘图及 PNG 保存。
- 新建 `E/README.md`：记录图形界面和命令行工具的使用方法。
- 验证结果：未知边长、未知方向角、未知内角及 GUI 示例均通过；示例四边形得到 `x=3` 并正确闭合。

### 2026-07-29：H 题钢珠 RGB-D 三维定位

- 新建 `H/camera_to_base.json`：定义 `camera_optical -> camera_base` 外参。当前旋转矩阵为单位阵、平移为零，即摄像头坐标系和摄像头底座坐标系完全重合。
- 新建 `H/geometry.py`：读取并校验外参，按 `p_base = R × p_camera + t` 完成三维点转换，同时提供 4×4 齐次矩阵。
- 新建 `H/depth_utils.py`：在 YOLO 钢珠框中心椭圆区域读取 RealSense 深度，过滤零值、超范围值和离群值；有效点不足时显式返回失败，避免误用背景深度。
- 新建 `H/ball_depth_tracker.py`：对齐 D435 深度流到彩色流，使用既有 YOLO/TensorRT 权重识别钢珠，输出钢珠像素中心、表面深度、摄像头坐标系三维点及摄像头底座坐标系三维点；支持画面叠加、标准输出 JSON 和 JSONL 文件记录。
- `H/ball_depth_tracker.py` 仅在 RealSense 管线成功启动后执行停止操作，保证相机未连接或启动失败时也能给出明确错误并正常退出。
- `H/ball_depth_tracker.py` 增加 TensorRT 失败自动回退：若 `.engine` 因 Jetson CUDA/统一内存不足无法加载，则自动载入现有 `best.pt` 并切换 CPU 推理；可用 `--no-cpu-fallback` 禁用。
- 新建 `H/start_ball_depth_tracker.sh`：固定使用项目的 `yolo-steel-ball` Conda 环境，并与原钢珠相机程序共用设备锁，避免两个进程同时占用 D435。
- 新建 `H/tests/test_geometry_and_depth.py`：覆盖当前单位外参、一般旋转平移、钢珠中心深度采样和无效深度拒绝。
- 新建 `H/tests/test_ball_location.py`：用模拟 YOLO 钢珠框和 RealSense 内参对象验证“像素中心→深度→摄像头三维点→底座三维点”的有效检测全链路。
- `H/tests/test_geometry_and_depth.py` 主动将 H 目录加入模块搜索路径，避免项目 Conda 环境忽略外部 `PYTHONPATH` 时测试导入失败。
- 新建 `H/README.md`：记录坐标轴定义、运行方式、输出字段、外参修改位置和金属钢珠深度空洞注意事项。
- 环境检查：系统 Python 缺少 `pyrealsense2` 和 `ultralytics`；项目 Conda 环境包含 `pyrealsense2`、Ultralytics 8.3.0、OpenCV 4.10.0、NumPy 1.24.4 和 Torch 2.1.0。
- RealSense 内参处理：不手工填写相机矩阵；每帧从彩色流 profile 直接读取当前分辨率对应的出厂标定内参，深度对齐到彩色图后用 RealSense 反投影函数求三维点。若后续切换 ROS，等价参数可从 `/camera/color/camera_info` 的 `K、D` 字段读取。
- `H/ball_depth_tracker.py` 启动后会将设备实际返回的彩色相机分辨率、`fx、fy、ppx、ppy`、畸变模型和系数打印一次，便于确认所用内参与当前流匹配。
- 首次实机检查：已识别 Intel RealSense D435，序列号 `254522076117`，固件 `5.15.1.55`；RGB/深度管线成功启动，深度比例为 `0.0010000000475 m/unit`。4 项坐标与深度单元测试全部通过。
- TensorRT 实机初测：当前 Jetson 仅约 1.48 GiB 可用统一内存，加载 `best.engine` 出现 NvMap error 12 / CUDA error 2；问题定位为内存不足而非相机取流或坐标变换错误，已增加 CPU 回退路径。
- CPU 回退实测：D435 连续采集 5 帧并正常输出 JSON/JSONL；当前画面未识别到钢珠，5 帧均明确返回 `ball_not_detected`，因此真实钢珠表面深度有效性仍待钢珠进入画面后验证。
- 内参实机读取结果（640×480 彩色流）：`fx=606.5006714`、`fy=605.7702637`、`ppx=325.7670593`、`ppy=256.5392456`，畸变模型为 `inverse_brown_conrady`。以上数值由 D435 当前流 profile 直接返回，未手工填写。
- 最终无相机测试结果：5 项测试全部通过，包括单位外参、一般旋转平移、中心深度采样、无效深度拒绝，以及模拟检测框的完整反投影与底座坐标转换。
- 剩余实机验证：D435 已连接且取流、内参读取、JSON 输出均已验证；还需把钢珠放入当前画面，确认金属表面有效深度比例和真实三维坐标，并在释放 Jetson 内存后复测 TensorRT 实时帧率。
- 钢珠大框误识别分析：用户日志中正常框为约 `35～43 px × 40～50 px`、面积占画面 `0.47%～0.70%`；偶发错误框为约 `613～636 px × 480 px`、面积占画面 `95.8%～99.5%`。
- `H/ball_depth_tracker.py` 增加检测框尺寸过滤：默认拒绝面积占比大于 3%、宽度占比大于 25%或高度占比大于 25%的框。被过滤框返回 `detection_filtered`，记录过滤数量，但不附带 `bbox_xyxy`，因此不会在启动窗口绘制；同帧存在正常小框时仍选择正常框。
- `H/ball_depth_tracker.py` 默认 RealSense RGB/深度流帧率由 30 FPS 改为 60 FPS；仍可通过 `--fps` 参数覆盖。
- `H/tests/test_ball_location.py` 增加“大框完全过滤”和“高置信度大框与正常小框同时出现时选择小框”两项测试。
- 大框过滤修改后共 7 项自动测试全部通过；过滤结果不包含 `bbox_xyxy`，已验证显示函数不会绘制被拒绝框。
- D435 60 FPS取流实测通过：可同时开启 640×480 RGB和深度流。按 `./H/start_ball_depth_tracker.sh --no-cpu-fallback` 的默认配置，TensorRT 本次成功加载；30帧连续推理正常，处理输出约为 25～35 FPS。这里的60 FPS是相机采集帧率，实际处理帧率仍受YOLO推理速度限制。

### 2026-07-29：H 题 PPR 管道识别

- 新建 `H/ppr_pipe_detector.py`：实现“灰度→高斯模糊→Canny→形态学闭运算→外轮廓”的基础流程，用最小外接矩形长度、宽度、长宽比和轮廓面积过滤短线及杂物，再用 `cv2.fitLine` 计算管道中心轴和端点。
- `H/ppr_pipe_detector.py` 增加 `HoughLinesP` 双边线配对：只接受足够长、角度近似平行、间距合理且沿轴向有充分重叠的两条线，以两条边线的几何中线作为管道轴线；霍夫结果需与主轮廓在角度、中心和长度上相符才用于校正。
- PPR检测输出包含中心像素、两个轴线端点、无方向倾角（归一化到 `[-90°, 90°)`）、像素长度、像素宽度、长宽比、得分和检测方法；支持窗口叠加、调试边缘窗口、标准输出 JSON及JSONL记录。
- 新建 `H/start_ppr_pipe_detector.sh`：使用项目视觉 Conda 环境和统一相机锁，默认开启 RealSense 640×480、60 FPS彩色流。
- 新建 `H/tests/test_ppr_pipe_detector.py`：用带短线、矩形等干扰物的合成图检查旋转管道、水平管道识别，以及短杂物拒绝。
- 更新 `H/README.md`：记录 PPR检测运行方式、处理流程、默认阈值、输出字段和调试参数。
- PPR模块加入后全部10项H题自动测试通过。D435实机以640×480、60 FPS连续运行30帧并正常输出JSON/JSONL；当前相机画面被近距离浅色平面占据，未出现完整PPR管道，因此30帧均正确返回 `ppr_pipe_not_detected`，真实管道角度和端点仍需将整根管道放入画面后复测。
- PPR卡顿诊断：用户以 `--show-debug` 运行时，PPR进程约占120% CPU，同时VS Code终端和渲染相关进程合计占用超过200% CPU，系统CPU空闲约3%。日志中霍夫候选中位数为0、最大为10，卡顿并非线段组合爆炸，主要来自逐帧JSON终端渲染、三个OpenCV窗口逐帧刷新和整机CPU竞争。
- `H/ppr_pipe_detector.py` 实时优化：相机继续采集640×480@60 FPS，默认将检测图降采样到320×240，所有像素阈值按比例换算，检测结果再映射回原始640×480坐标；可用 `--process-scale` 调整。
- PPR程序默认 `--print-every 0`，不再逐帧向终端刷JSON；需要数据时可显式设置打印间隔或使用JSONL文件。`--show-debug` 的边缘窗口默认每5帧刷新一次，并将霍夫原始线段限制为最长24条，避免极端杂乱画面的组合开销。
- PPR性能复测：同一640×480实机画面上，全分辨率检测中位耗时约90.97 ms（约11 FPS）；默认0.5倍检测图中位耗时约22.27 ms（纯算法约44.9 FPS）。D435连续120帧端到端、无显示窗口实测约20.5 FPS；相比用户最初开启逐帧日志和三个窗口时的7～8 FPS已明显改善，剩余开销来自取帧、系统CPU竞争和显示。
- `H/ppr_pipe_detector.py` 增加 `--mode full|light` 参数化方案切换。`full`保留灰度、Canny、闭运算、轮廓及霍夫双边线校正；`light`改为代码独立的普通OpenCV方案：灰度、高斯、Otsu自动二值化、亮/暗双极性连通区域分割、简单闭运算、细长矩形筛选和 `fitLine`。轻量分支不调用Canny、Hough或完整方案的候选组合函数。
- 轻量二值方案使用长度、宽度、长宽比、连通面积、画面面积占比和矩形填充率过滤杂物，输出方法名为 `binary_fitline`，并给出采用的 `bright` 或 `dark` 二值极性。两种模式的中心、端点、角度及JSON接口保持一致。
- `H/tests/test_ppr_pipe_detector.py` 增加独立轻量模式测试，分别验证亮色管/暗背景和暗色管/亮背景的二值分割、中心轴与角度。
- 独立轻量二值方案加入后H题共12项自动测试全部通过。D435以640×480@60采集，`--mode light --no-display` 连续120帧端到端实测约39.7 FPS；当前画面没有完整PPR管，因此正确返回 `ppr_pipe_not_detected`。
- 轻量方案“识别即卡顿”专项诊断：100次离线对比中，空画面检测中位2.13 ms、成功识别管道3.11 ms、随机噪声3.18 ms；成功结果绘图中位2.62 ms。识别成功分支未发现计算爆炸，卡顿更可能来自高负载VS Code桌面、X11及多个窗口渲染竞争。
- `H/ppr_pipe_detector.py` 增加桌面响应保护：默认限制OpenCV为2个CPU线程、主窗口每2帧刷新一次、轻量方案每种二值极性最多检查面积最大的64个轮廓；JSON增加 `processing_ms` 纯算法耗时，便于区分算法卡顿和显示卡顿。相关参数为 `--opencv-threads`、`--display-every` 和 `--max-light-contours`。
- 桌面响应保护后实机复测：以轻量模式和主显示窗口连续运行300帧，成功识别出第170、190、280、290帧，程序未卡死；四个成功帧的纯算法耗时分别约12.49、4.91、4.38、4.49 ms，未比普通帧显著增加。含窗口和每10帧一次JSON输出的整体处理约28 FPS，证明识别成功分支本身不是卡死原因。

### 2026-07-29：H 题局域网视频推流

- 新建 `H/udp_video_stream.py`：通过GStreamer将BGR画面转换为NVMM NV12，使用Jetson `nvv4l2h264enc`硬件编码为H.264，再经RTP/UDP发送到指定PC地址。后台队列容量为1，拥塞时丢旧帧保最新帧，避免网络阻塞视觉主循环。
- `H/ball_depth_tracker.py` 和 `H/ppr_pipe_detector.py` 均增加 `--stream-host、--stream-port、--stream-fps、--stream-bitrate` 参数，可推送640×480纯彩色画面；未设置 `--stream-host` 时不创建编码进程。
- 新建 `H/pc_receiver.sdp`，PC可用VLC打开该文件接收默认UDP 5600视频；`H/README.md` 同时给出低延迟GStreamer接收命令和Windows防火墙说明。
- 新建 `H/tests/test_udp_video_stream.py`：检查IP和端口配置校验、NVIDIA H.264编码器、RTP负载及UDP目的地址命令生成。
- 本机能力检查：`gst-launch-1.0`、`nvvidconv`、`nvv4l2h264enc`、`rtph264pay` 和 `udpsink` 均可用；640×480原始BGR帧经NVIDIA硬件编码到RTP管线已用本地 `fakesink` 实测通过。
- 根据使用要求，视频流不叠加钢珠识别框、三维坐标、深度值、PPR管道轴线或检测状态；识别结果不会随视频发送到PC。
- 完成90帧本机UDP环回实测：纯BGR画面经 `nvv4l2h264enc` 编码、RTP/UDP发送，再由 `nvv4l2decoder` 接收解码，全链路无推流错误；修改后H目录14项自动测试全部通过。
- 新增 `H/start_offline_hotspot.sh` 和 `H/stop_offline_hotspot.sh`：比赛现场由Jetson的 `wlan0` 建立独立WPA2热点 `NUEDC-H`，固定地址 `192.168.50.1/24`，NetworkManager共享模式为PC提供DHCP；不依赖校园网、路由器或互联网。停止脚本支持恢复指定的已保存Wi-Fi连接。由于启动会主动断开当前校园网，本次只完成能力检查和脚本静态验证，不在校园网连接期间自动切换。
- PC默认使用热点DHCP地址，范围为 `192.168.50.10`～`192.168.50.254`；接收脚本显示本次实际地址，Jetson的 `--stream-host` 必须与之完全相同，不再将旧示例 `192.168.50.2` 描述为固定PC地址。
- 新增 `H/PC` 接收端目录：`receiver.sdp` 描述H.264/RTP UDP 5600视频，`start_receiver.bat` 调用 `start_receiver.ps1`；PowerShell脚本检查PC是否取得 `192.168.50.x` 地址、显示应传给Jetson的目的IP、在管理员模式下建立UDP 5600防火墙入站规则，并以100 ms缓存参数启动VLC。`H/PC/README.md` 记录Windows完整操作、Linux GStreamer接收命令及故障排查流程。
- 生成可直接复制到PC并解压使用的 `H/NUEDC_H_PC_receiver.zip`，压缩包完整性检查通过；其中包含接收脚本、SDP和操作说明。
- 新增 `H/camera_video_stream.py` 和 `H/start_camera_video_stream.sh` 作为独立纯视频方案：只启用RealSense彩色流，默认640×480@60、3 Mbit/s，经后台队列和Jetson硬件H.264编码发送；不导入或加载YOLO、TensorRT、Torch、深度流及识别算法。该方案用于PC只需观看视频的场景，避免为推流占用TensorRT内存。
- 用户原命令失败诊断：即使关闭推流、只执行1帧钢珠识别，TensorRT仍报告CUDA out of memory；当时系统RAM占用约4370/6833 MB、连续大内存块仅 `4×4 MB`，说明故障源是YOLO TensorRT引擎初始化内存不足，不是UDP网络。独立纯视频程序在同一系统负载下以640×480@60配置实机连续发送120帧成功，NVENC无错误，观测采集约35～40 FPS。
- 热点收不到视频复核：NetworkManager日志确认PC `LAPTOP-2G9AJLHM` 已从Jetson热点取得 `192.168.50.115`，而测试推流目的地址使用了旧示例 `192.168.50.2`。RTP/UDP为单播，目的IP不一致时PC不会收到数据；文档已改为先读取PC脚本显示的实际地址再启动推流。VPN的局域网阻断或kill switch可能造成次要影响，首次联调应关闭VPN排除干扰。
- 长时间推流卡顿优化：视频没有落盘，发送队列容量仍为1；卡顿主因按实测判断为相机/转换链路只能持续约35～40 FPS却使用60 FPS RTP时间戳，长期形成发送到达时间与媒体时钟漂移，叠加Wi-Fi UDP丢包后VLC缓冲不足。纯视频程序改为相机640×480@60采集、单调时钟稳定30 FPS发送，只取最新帧且不突发补帧；发送器增加提交、写入和队列丢帧统计，状态日志显示采集FPS、发送FPS和累计编码队列丢帧。PC端VLC抗抖缓存由100 ms调整为200 ms。
- 编码线程进一步改为严格按配置帧率输出：输入慢于30 FPS时重复上一张最新帧，输入快于30 FPS时用容量1队列替换旧帧，编码器启动或调度落后时恢复当前节拍而不突发补帧。新增实际编码输出帧数与重复帧统计，从机制上保证RTP媒体时钟和UDP发送墙钟长期一致，同时保持内存占用恒定。
- 优化后实机连续900个相机帧测试：稳定段相机采集约55.3～56.5 FPS，最新帧刷新约28.4～29.2 FPS，硬件编码实际输出保持29.6～29.8 FPS；编码线程按需重复最近帧，容量1队列替换累计基本不增长，14项H目录自动测试全部通过。
- 按当前PC配置，将独立纯视频程序的默认目的地址设为 `192.168.50.43:5600`，因此 `H/start_camera_video_stream.sh` 可无参数启动；新增兼容简写 `H/starcamera_video_stream.sh`。命令行显式传入 `--stream-host` 或 `--stream-port` 时仍可覆盖默认值，`--help` 会显示各参数默认值。
- 新增 `H/capture_photos.py`、`H/start_capture_photos.sh` 和照片目录 `H/captured_photos`：RealSense默认以640×480@60预览，按空格或S将不带预览文字的原始彩色帧以JPEG质量95保存，按Q或Esc退出；照片使用 `前缀_毫秒时间戳_六位序号.jpg` 命名避免覆盖。启动脚本复用相机文件锁，防止拍照与识别/推流同时占用D435。
- 拍照窗口卡顿诊断时无残留相机进程、可用内存约3.0 GiB，但多个VS Code进程持续高CPU占用、六核总体约55%～68%忙碌；拍照脚本原先又以60 FPS逐帧复制、绘字和刷新窗口。轻量化后默认改为640×480@30采集、每2帧刷新一次窗口（约15 FPS预览）、OpenCV限制1个线程，按键仍每帧响应，保存始终使用最新的无叠加完整原图；新增 `--display-every、--opencv-threads、--max-frames` 参数。
- 首轮轻量化实机300帧仍耗时约30秒，确认主要阻塞位于X11窗口事件处理。预览进一步默认缩放为320×240，并只在每2帧实际刷新窗口时调用 `waitKey`，不再让每个相机帧进入GUI事件循环；新增 `--preview-scale` 参数，照片尺寸不受预览缩放影响。
- 当前OpenCV 4.10支持非等待式 `pollKey()`，拍照脚本用它替换 `waitKey(1)`，使按键和窗口事件处理不再人为暂停相机循环。
- 160×120与320×240预览对比没有提速，确认当前X11环境中每次 `imshow` 存在近似固定提交开销；默认窗口刷新间隔进一步设为每4个相机帧一次（目标约7.5 FPS），将GUI调用减半，按键响应仍约0.1秒，完整照片不受影响。
- 最终配置实机300帧运行时间约21.6秒，其中包含RealSense启动和15帧自动曝光预热；相同流程近乎关闭显示仍约17.2秒，说明剩余大部分时间来自相机启动/取帧和当前系统高负载，而非照片保存或内存积压。H目录14项自动测试保持通过。
- 钢珠程序看似比拍照窗口流畅的原因是YOLO推理天然限制了 `imshow` 调用频率，而拍照程序无推理阻挡、会更频繁向高负载X11桌面提交画面。拍照默认窗口刷新进一步限制为每15个相机帧一次（约2 FPS），相机仍以30 FPS更新待保存原图；因此降低桌面压力不会降低照片分辨率或使保存内容停留在旧帧。
- 约2 FPS预览配置实机300帧总运行时间约19.4秒（包含相机启动和预热），相比此前约21.6秒继续下降；修改后H目录14项自动测试全部通过。
- 钢珠识别默认权重切换为 `/home/pangolin/Documents/xwechat_files/wxid_xrsox4psckd822_af54/msg/file/2026-07/v3.pt`；`H/ball_depth_tracker.py` 不再默认优先选择 `best.engine`。原有 `/home/pangolin/Downloads/best.engine` 和 `best.pt` 文件均保留且仍可通过 `--weights` 手动指定；显式TensorRT引擎失败时的CPU回退目标同步改为默认 `v3.pt`。
- `v3.pt` 文件读取验证通过，模型类别为 `{0: steel_ball}`；当前高桌面负载下首帧CUDA推理仍出现out of memory，因此自动CPU回退范围由仅TensorRT引擎扩展到所有非CPU权重启动失败，`--no-cpu-fallback` 仍可禁止回退。
- 2026-07-30：将新权重 `/home/pangolin/Documents/xwechat_files/wxid_xrsox4psckd822_af54/msg/file/2026-07/best(1).pt` 复制到项目并重命名为 `H/weights/steel_ball_best.pt`，SHA-256均为 `e44621f19377cab0d6ec59c048ff5b0ca8a84b8f99ebbc127e21f95b5c74c7a7`，确认复制一致。钢珠跟踪器默认权重及GPU失败后的CPU回退权重均切换为该项目内文件；微信源文件、旧 `v3.pt`、`best.engine` 和 `best.pt` 均未删除。
- 2026-07-30：旧权重统一复制到 `H/weights` 管理：`v3.pt` 重命名为 `steel_ball_v3.pt`，旧 `best.pt` 重命名为 `steel_ball_best_legacy.pt`，旧 `best.engine` 重命名为 `steel_ball_best_legacy.engine`；三个副本SHA-256分别为 `a8ace794752e7f4f85079e56497c08c9554bee078b65b52ade0e2350181a1eaf`、`bd1a1a447bfcece5fcd71cc4fbc3451bfd664aec1330bf86a992541d8999dc8d`、`d78a50a8443d1517f167e817027e42c6d26fbdcdc8375d10368aff1dffffd0ac`，均与原文件一致。新增 `H/weights/README.md` 记录当前默认、历史版本、来源、哈希和切换命令，所有原位置文件继续保留。
- 2026-07-30：将 `/home/pangolin/Documents/xwechat_files/wxid_xrsox4psckd822_af54/msg/file/2026-07/v4_interrupted_best.pt` 复制为 `H/weights/steel_ball_v4_interrupted_best.pt` 并设为钢珠识别及CPU回退的新默认权重；源文件和项目副本SHA-256均为 `43d0e30b356999b3a0301144ddf19310da297afe66d0d4590e60e7ff41b375e2`。此前 `steel_ball_best.pt`、`steel_ball_v3.pt`、legacy PT和legacy engine均保留。
- 2026-07-30：钢珠深度和坐标改为球心语义。RealSense中心ROI采样值保留为 `surface_depth_m`，默认增加钢珠半径0.005 m得到球心 `depth_m`，再用球心深度和检测框中心像素反投影；因此 `camera_point_m` 与经外参变换后的 `camera_base_point_m` 均表示球心坐标。新增 `ball_radius_m` 输出及 `--ball-radius-m` 参数，默认0.005 m，并增加球面1.000 m应得到球心1.005 m的全链路测试。
- 2026-07-30：将 `/home/pangolin/Documents/xwechat_files/wxid_xrsox4psckd822_af54/msg/file/2026-07/best(2).pt` 复制为 `H/weights/steel_ball_best_2.pt`，并设为钢珠识别与GPU失败CPU回退的新默认权重。源文件和项目副本SHA-256均为 `bad647dfbaaa381234e55ee8a47b48c8cc2b74a5f94771b4e69a007f99fb0697`；此前v4 interrupted、best、v3及legacy权重全部保留。
- 2026-07-30：确认并固化多球选择规则：每帧先过滤尺寸异常框，再在所有合格钢珠框中按置信度取最大值，只输出该球的像素、球心深度、摄像头坐标和底座坐标。新增“两颗正常球仅选择0.92置信度球、不输出0.70置信度球”的单元测试。
- 2026-07-30：依据Intel RealSense官方光学坐标定义（正X向右、正Y向下、正Z向前，右手系）和实际安装描述，将底座系定义为正X向前、正Y向左、正Z向上，并更新 `H/camera_to_base.json`：`x_base=z_camera、y_base=-x_camera、z_base=-y_camera`，即 `R_base_from_camera=[[0,0,1],[-1,0,0],[0,-1,0]]`。该矩阵正交且行列式为1；因尚未提供相机光心相对底座原点的位置，平移暂保留零。测试增加三个相机单位轴和组合点到各底座轴的映射，并更新球心全链路期望值。
- 2026-07-30：在上述轴定义基础上加入实际安装外参：相机光心比底座原点高0.25 m，镜头由底座正X水平前方下俯30°，假设无前后/左右平移。更新 `R_base_from_camera=[[0,-0.5,0.8660254],[-1,0,0],[0,-0.8660254,-0.5]]`、`t=[0,0,0.25] m`；对应 `x_base=-0.5y_camera+0.8660254z_camera、y_base=-x_camera、z_base=-0.8660254y_camera-0.5z_camera+0.25`。同步更新原点、三个单位轴、组合点和球心全链路测试。
- 2026-07-30：新增 `H/calibrate_camera_ground.py` 和 `H/start_ground_calibration.sh`，用于相机只观察水平地面时反推安装高度、下俯角和横滚角。脚本从RealSense深度点云随机降采样，使用RANSAC过滤非地面点，再对内点SVD精拟合；由平面到相机原点距离得到高度，由相机系地面法向量得到俯仰和横滚，并输出内点比例、RMSE、完整4×4齐次矩阵及按底座轴规则生成的JSON建议，不自动改写正式外参。按安装约束固定镜头朝底座正前方及水平平移为零；终端只输出当前实测的 `z、roll、pitch`，不设置名义值或计算偏差。新增合成地面和非零横滚测试。
- 2026-07-30：地面标定及既有H题模块共18项自动测试全部通过；实机确认Intel RealSense D435（序列号 `254522076117`）深度流和点云接口正常，单帧可筛得12000个有效候选点。当前实机画面并非单一地面，五次拟合内点比例约10.0%～12.8%，均被默认55%质量门限拒绝，证明脚本不会在观测条件不满足时给出错误外参；需将画面完全对准地面后正式采集。
- 2026-07-30：采用正式地面标定的10组稳定样本更新 `H/camera_to_base.json`：相机高度 `z=0.262249 m`、下俯 `pitch=37.98°`、横滚 `roll=-9.46°`，偏航按安装约束固定为镜头朝底座正X，水平平移为零。实测地面内点比例约70.6%～71.4%、平面RMSE约1.57～1.61 mm；外参更新为 `R=[[-0.101179197,-0.606963894,0.788262394],[-0.986389035,0.164428316,0],[-0.129612658,-0.777533382,-0.615339255]]`、`t=[0,0,0.262249] m`，并同步更新相机单位轴、组合点及钢珠球心到底座坐标的全链路测试期望值。
- 2026-07-30：补充项目根目录 `.gitignore`：排除Python缓存、本地Conda环境、RealSense及CMake编译产物、ROS 2的build/install/log、编辑器配置、运行日志、YOLO输出、拍照数据、生成压缩包和大体积PT/Engine/ONNX模型；保留H/E源码、启动脚本、测试、文档、`H/weights/README.md` 及正式外参 `H/camera_to_base.json`。
- 2026-07-30：修复首次Git提交中的嵌套仓库警告：提交 `45c671c` 已成功创建，第二条 `cannot lock ref 'HEAD'` 只是VS Code并发重复提交造成，仓库和HEAD无损坏且无残留锁文件。新增 `.gitmodules`，将已记录为gitlink的 `ros2_ws/src/realsense-ros` 正式声明为来源 `https://github.com/realsenseai/realsense-ros.git` 的子模块，并忽略其本机未跟踪的 `COLCON_IGNORE` 状态，确保外层仓库可正常克隆和查看状态。
- 2026-07-30：新增H题钢珠位置闭环控制模块。`H/ball_control_config.json` 统一记录球心零点、25 cm管长、4.09 g质量、±30°物理限角、25 Hz控制频率、50 Hz串口频率、状态估计、串级PID和运动模型参数；未标定 `zero_point_base_m` 时真实控制拒绝启动。`H/calibrate_ball_zero.py` 通过现有YOLO+RealSense球心底座坐标收集50个静止样本，以中位数/MAD剔除离群并写入固定零点。位置按用户定义取球心与固定零点的三维距离，正方向从零点指向电机端。
- 2026-07-30：`H/ball_depth_tracker.py` 在图像转换和YOLO推理前新增 `capture_monotonic_ms` 与RealSense帧时间戳，并输出处理延迟，避免用推理完成墙钟差分速度。`H/ball_control.py` 新增可变采样间隔的常加速度Kalman滤波器，状态为位置/速度/加速度，包含连续白噪声jerk过程模型和创新门控；控制主要使用位置与速度，加速度用于辨识和可选扰动观测。
- 2026-07-30：实现必选串级PID：外环位置PI输出限幅目标速度，内环速度PI直接输出倾角，按“正角抬升电机端、球向零点、x减小”的机械定义加入正确负号；两级积分限幅、角度限幅和每周期最大1.5°变化约束。`H/ball_control_runtime.py` 默认25 Hz计算、±10°安全工作限角，视觉超过0.20 s无有效测量立即归0，目标范围限制为0～25 cm；默认只试算，必须显式 `--enable-serial` 才驱动电机。
- 2026-07-30：实现实验性束搜索非线性MPC。预测模型为 `xddot=-b*sin(theta)-c_c*tanh(v/v_eps)-c_v*v+d`，默认实心球纯滚动名义增益 `b=5g/7`；18步预测、每层5种角度变化并保留96条最低代价路径，原生满足工作角限位和相邻变化量限制，对预测越出0～25 cm强惩罚。曾验证SciPy-SLSQP单次约70～90 ms不满足25 Hz，最终束搜索实机纯控制基准约10～15 ms，不依赖CVXPY/OSQP；离线双向阶跃仿真后将速度权重由80提高到150，消除低速度惩罚导致的明显端点越界，视觉加速度DOB默认关闭。
- 2026-07-30：按单片机协议重写 `serial/send.py` 并新增 `H/angle_serial.py`：发送8字节帧 `[0x92,符号,整数,两位小数,0,0,0,0x29]`；正负号为0x00/0x01，十进制ROUND_HALF_UP量化到0.01°，消除负零并拒绝超出±30°、NaN/Inf。串口持久独占打开，后台单调时钟默认50 Hz重发最新角，检测短写和异步故障，持续发送工具及闭环控制正常退出前额外发送一次0°；9600 8N1占用约4000 bit/s，单片机可通过0x92/0x29重新同步帧边界。
- 2026-07-30：新增 `H/calibrate_pipe_friction.py` 固定倾角滚落实验。脚本等待首个有效球位置后提示释放，保存原始底座球心/位置/置信度CSV，自动选择远离端点的连续运动段并稳健拟合 `x=x0+v0*t+0.5*a*t²`，输出净加速度、经验集总 `b=|a|/|sin(theta)|`、质点模型等效扰动力及理想实心球剩余损耗；`--update-model` 可显式写入经验增益。明确一次+10°单方向实验无法唯一分离滚动惯量、库仑/滚阻、黏性阻力和静摩擦，完整辨识需±5°/±10°/±15°多次双向实验。
- 2026-07-30：新增 `H/BALL_CONTROL.md`，整理坐标符号、控制模型、零点标定、串级PID与MPC原理、完整启动命令、调参顺序、单片机协议以及需测量的零角、方向、视觉噪声/延迟、起滚临界角、角度—加速度、速度阻力、局部卡滞和安全制动等性质。新增23项控制/串口/标定测试，与原H题测试合计41项全部通过；测试覆盖精确8字节帧编码、50 Hz假串口、短写、PID/MPC方向与约束、Kalman噪声/离群、零点稳健统计和合成10°轨迹恢复，未连接真实电机。
- 2026-07-30：根据钢珠YOLO实测只有15～20 FPS修正控制时序。`H/ball_control_runtime.py` 和 `H/simulate_ball_control.py` 只在一条新鲜且被Kalman接受的视觉测量到达时更新一次控制器，帧间保持上一倾角；25 Hz循环仅做超时监督，串口独立50 Hz重发最新8字节指令。控制器积分和每步1.5°变化限制均按真实视觉采样间隔计算，终端新增 `control_updated_from_new_vision`，明确区分视觉控制更新率、监督率和串口重发率。
- 2026-07-30：补强实时安全时序。实时控制强制要求 `capture_monotonic_ms`，拒绝缺失、重复、倒退、未来或采集后超过0.25 s的记录；有效性同时检查采集年龄和读取到达年龄，延迟外推仅使用常速度且终端显示未截断的真实测量年龄。视觉超时会归0、重置控制器并清除连续稳定计时；识别子进程非零退出不再误报成功。退出串口顺序改为先将待发值设0并停止后台线程，再同步发送最后一帧0°，消除后台旧非零帧覆盖归零帧的竞争。
- 2026-07-30：围绕1 cm比赛判负底线实现并调参串级PID安全策略。`CompetitionTargetMonitor` 按初始来向锁存单侧失败边界，内部成功要求误差≤0.3 cm、速度≤0.8 cm/s连续0.5 s。PID最大参考速度降为0.035 m/s并加入距离制动包络；静摩擦补偿改为从0缓升、只充当PID同向最小起滚角，禁止固定角直接叠加。采用两阶段目标：先停在来向侧0.6 cm，必须连续低速0.6 s后，才以0.1 cm/s逐步把内部偏置减到0.1 cm，避免视觉速度偶发低估时过早释放安全余量。
- 2026-07-30：新增并完善 `H/simulate_ball_control.py` 非线性闭环仿真，显式模拟15～20 FPS视觉、40～120 ms延迟、0.8～2.5 mm噪声、0～6%丢帧、0.25 s失视归零、有效重力增益/库仑阻力/黏性阻力/恒定偏置不确定性；仿真只在新视觉帧更新控制器，并以真实被控对象位置判定是否越过1 cm。两个独立随机种子各100次、每次20 s验证均为0次比赛越界，分别62/100和53/100次最终进入±0.3 cm连续稳定，最坏错误侧越过0.943 cm；固定8个联合端点压力场景仍有4/8失败、最坏1.069 cm，明确记录为完成实物摩擦/增益辨识前不能保证的边界。未标定名义MPC在20次同类随机仿真中17次失败，因此仅保留为辨识后的实验对照，不允许现阶段直接上机。
- 2026-07-30：控制测试扩展到55项并全部通过，新增正反方向1 cm失败锁存、0.3 cm连续稳定、失视清除稳定计时、制动速度对称性、起滚补偿方向、两阶段目标连续低速驻留、15/17.5/20 FPS测量触发次数、固定压力场景定义、实时采集时间必需性、延迟外推上限与真实年龄报告等测试；完成Python语法检查，全程未打开真实相机、串口或电机。
- 2026-07-30：新增 `H/tuning_diagnostics.py`，并为 `H/ball_control_runtime.py` 增加默认关闭的 `--tuning-debug` 串级PID实机调参开关。开启后只在新鲜且被Kalman接受的视觉控制更新上统计球位置、实际/内部误差、速度、外环参考速度、最终倾角、处理/有效FPS、测量接受率及延迟；每2秒输出状态，根据视觉低帧率/低接受率/断帧/高延迟、不起滚、当前速度指令方向不一致、接近1 cm错误侧边界过快、持续振荡和响应慢给出限频且一次只改一个参数的建议。诊断不会自动修改配置、PID状态、倾角或串口数据；安全试运行不生成基于电机运动的PID建议，实验性MPC也拒绝套用串级PID建议。
- 2026-07-30：调参输出使用独立后台latest-slot线程，控制循环只提交不可变数字快照；终端阻塞时丢弃旧诊断快照而不反压15～20 FPS控制更新或50 Hz串口。处理FPS用识别帧号差计算，有效FPS用控制线程累计接受序号计算，诊断自身丢样不会误报低有效率或视觉断帧；一直漏检、深度无效、Kalman拒绝或识别进程无输出时仍每2秒输出最近帧、原因和处理耗时。修正了越过目标后按当前 `v_ref` 回拉被误报方向反的问题，并禁止建议把0.6 cm预停偏置增加到二阶段解锁范围之外。新增调参规则、后台慢终端、无效视觉心跳、双向边界、恢复方向及诊断丢样测试；H目录共70项自动测试全部通过，Python语法和启动参数帮助检查通过，全程未打开真实相机、串口或电机。
- 2026-07-30：修复零点标定在Jetson CUDA统一内存不足时把识别器启动失败误报成“超时前0个样本”的问题。`H/calibrate_ball_zero.py` 新增独立的60秒识别器启动超时，收到第一条JSON帧后才开始计算有效样本采集超时，并通过后台读取避免无输出时永久阻塞；识别子进程若被SIGKILL会明确提示通常为Jetson内存不足，其他非零退出码也会保留。采样提示后才启动识别器，失败不会覆盖已有零点；文档补充CPU静态标定降级命令，明确CPU模式不用于实时闭环。
- 2026-07-30：调整1 cm比赛失败后的实时行为：`CompetitionTargetMonitor` 仍按初始来向锁存 `competition_failed` 并保留失败边界用于统计，但 `H/ball_control_runtime.py` 默认不再把倾角清零、重置PID或退出程序，而是保持闭环并输出反向回拉控制，使钢珠尽量返回目标；失败提示只打印一次。新增显式 `--stop-on-competition-failure` 参数用于需要“越线立即0°并退出”的测试，失视归零、±30°物理限位和工作限角均保持不变。
- 2026-07-30：将 `/home/pangolin/Documents/xwechat_files/wxid_xrsox4psckd822_af54/msg/file/2026-07/v5.pt` 复制为 `H/weights/steel_ball_v5.pt`，源文件与项目副本SHA-256均为 `4498de7d983f0a4a96bdcdf15151e2be188888ef2d22f3b6b4e6bbd02a1b9486`。钢珠识别默认权重以及GPU失败后的CPU回退权重同步切换为v5；此前best(2)、v4 interrupted、best(1)、v3及legacy权重全部保留。
- 2026-07-30：精简钢珠控制器默认终端状态：输出频率由每5个25 Hz监督周期一行改为每25周期一行（约1 Hz），默认 `compact` JSON仅保留控制有效性、目标位置、当前位置、位置偏差、速度、按单片机协议量化后的角度命令、串口启用状态、稳定状态和比赛失败记录。新增 `--telemetry full` 恢复原完整滤波/补偿/时序/串口字节字段，`--print-every 0` 仍可完全关闭输出。
- 2026-07-30：按实机观察需求进一步删除默认精简JSON中的 `serial_enabled`、`settled` 和 `competition_failed`，现在每行仅包含 `valid、target_cm、position_cm、error_cm、velocity_cm_s、command_deg` 六项；这些安全和运行状态仍保留在 `--telemetry full` 完整模式以及独立异常提示中，不改变控制逻辑。
- 2026-07-30：新增 `H/motor_angle_test.py` 与 `H/start_motor_angle_test.sh`，可不启动相机直接交互输入电机目标倾角，或以位置参数持续发送单一角度；默认50 Hz、`/dev/ttyUSB0`、9600 baud和±10°测试限幅，可显式放宽但不超过±30°，输入q/Ctrl+C正常退出时归零。审查并修复 `serial/send.py` 持续模式的退出竞争：旧实现可能在同步0°后由后台线程补发一帧已缓存的旧非零角，现统一为先将待发值设0°、停止周期线程、最后同步发送0°；单次 `--once` 语义保持不追加归零帧。
- 2026-07-30：用户再次确认电机下发协议必须保留 `HEADER=0x92` 和 `FOOTER=0x29`，中间为原六位数据；已同步修正公共编码器、闭环控制器、`serial/send.py`、交互电机测试工具、终端量化角显示及全部协议测试。标准示例为 `-12.34° -> 92 01 0C 22 00 00 00 29`。
- 2026-07-30：电机串口增加连接初始化使能：每次打开串口后、发送第一帧角度命令前，公共 `PeriodicAngleSender` 自动且仅发送一次 `92 4F 4B 00 00 00 00 29`，其中 `4F 4B` 为ASCII大写 `OK`；随后才发送0°或目标角并进入50 Hz周期发送。`serial/send.py`、交互电机测试工具和视觉闭环控制器共用该逻辑，重新打开串口会重新使能，同一连接不在每帧重复。
- 2026-07-30：修复直接用系统 `python3 serial/receive.py` 时因缺少pyserial而显示无指导异常的问题：确认项目环境已安装pyserial 3.5，新增 `serial/start_receive.sh` 固定使用项目环境，脚本误用系统Python时会提示正确命令。同时修复回传帧解析中 `bytearray.find()` 错把整数帧头作为子串导致 `--framed` 模式类型错误的问题；接收方向仍保持原 `0x76 + 6字节 + 0x67`，与电机下发 `0x92 ... 0x29` 分开。
- 2026-07-30：新增 `serial/start_send.sh`，与 `serial/start_receive.sh` 一样固定使用项目内含pyserial 3.5的Python环境。命令接受角度及原 `send.py` 的串口、波特率、频率和单次参数；持续模式先自动发送一次 `92 4F 4B 00 00 00 00 29` 电机使能帧及一次0°初始化帧，再以默认50 Hz发送角度帧，Ctrl+C正常退出时发送最终0°。
- 2026-07-30：明确串口帧为原始二进制字节而非ASCII十六进制文本：例如+2.00°实际由 `serial.write(bytes([0x92,0x00,0x02,0x00,0x00,0x00,0x00,0x29]))` 一次写入8字节。为避免终端简写 `92 00 02...` 引起歧义，`serial/send.py` 和交互电机测试工具的回显统一增加 `0x` 前缀；线上发送内容不变。
- 2026-07-30：按电机实际使能协议，将串口连接初始化帧从小写ASCII `ok`（`92 6F 6B 00 00 00 00 29`）统一更正为大写ASCII `OK`（`92 4F 4B 00 00 00 00 29`），并将代码中的握手语义重命名为电机使能。公共发送器、视觉闭环、`serial/send.py`、交互测试工具、回显、文档和假串口测试全部同步；每次打开串口只使能一次，之后仅发送角度帧。
- 2026-07-30：补全电机启动时序：每次打开串口后先且只发送一次使能帧 `92 4F 4B 00 00 00 00 29`，立即再发送一次0°初始化帧 `92 00 00 00 00 00 00 29`，完成后才按原逻辑发送目标角度帧。该顺序下沉到公共 `PeriodicAngleSender`，因此视觉闭环、`serial/start_send.sh`、单次发送和交互电机测试全部一致；假串口测试验证两帧顺序、各一次及后续角度帧。
- 2026-07-30：按实机反馈提高串级PID远距离驱动力。原配置最大参考速度3.5 cm/s配合速度环比例增益25仅产生约0.875°原始PID角，实际长期被5°静摩擦补偿主导。曾尝试全局把速度环比例增益提高到250并把补偿提高到10°，但两个各100次随机仿真分别出现85和93次1 cm越界，最坏错误侧越过2.7433 cm，明确否决该全局高增益方案。最终改为分段远距离增强：内部误差≥5 cm且球速≤0.5 cm/s时将同方向驱动提高到10°，球滚起来或进入5 cm内立即恢复原25速度增益和5°补偿；±10°工作限角、每新视觉帧1.5°变化限制和近目标制动保持不变。测试覆盖双向远距离10°及近距离不超过5°。
- 2026-07-30：用户明确要求本轮实机调试先不考虑超调、继续提高激进程度；分段远距离增强因此取消球速退出条件，内部误差≥5 cm期间无论球速均持续输出同方向±10°，仅在误差进入5 cm内后恢复普通PID、5°补偿和制动。保留±10°默认工作限角和每新视觉帧1.5°变化限制；新增远距离球速3 cm/s时仍达到10°的测试。此前仿真超调数据保留作为后续收敛调参依据，本轮不据此削弱远距离命令。
- 2026-07-31：比赛控制器默认启用内嵌视频推流，目的地址设为 `192.168.50.199:5600`；`H/camera_video_stream.py` 的独立纯视频默认地址同步改为 `.199`。`H/ball_control_runtime.py` 新增写在分隔符前的 `--stream-host` 覆盖项及 `--no-stream` 关闭项，控制器复用内部钢珠识别器的同一RealSense彩色帧，不启动第二个相机进程。
- 2026-07-31（已被下一条中心坐标定义取代）：曾按管道物理零边缘定义输入，并用0.5 cm球半径换算内部目标；该过渡方案不再是当前控制接口。
- 2026-07-31：按最新要求将控制器对外位置改为以管道几何中心12.5 cm为0的有符号坐标，朝原零点端为负、朝原25 cm端为正。零点标定记录的是物理刻度0.5 cm处的球心，所以该点在新坐标中为 `-12.0 cm`；配置使用 `target_coordinate_center_m=0.12`，换算为 `target_internal=target_centered+12.0 cm`，不再重复扣除球半径。球心受两端0.5 cm半径限制，目标输入范围为 `-12.0～+12.0 cm`；零点标定脚本的终端提示同步改为物理0.5 cm/新坐标-12.0 cm，现有零点数据无需重采；控制终端位置、目标、失败边界和调参诊断均显示新坐标，PID/MPC内部仍使用从标定球心起算的非负位移。
- 2026-07-31：按新执行器接口将倾角改为仅供控制器内部及终端显示，串口线上发送电机端升降距离。统一理想换算 `h_mm=250*tan(theta)`：正倾角/正位移表示电机抬高，负倾角/负位移表示下降，0°对应零位；8字节帧的符号、整数和两位小数字段现在表示毫米。位移限制为 `±99.99 mm`，倾角接口和配置硬限位改为 `±21.80°`；例如 `+10° -> +44.08 mm -> 92 00 2C 08 00 00 00 29`，`+21.80° -> +99.99 mm -> 92 00 63 63 00 00 00 29`。闭环、手动电机测试和 `serial/send.py` 复用同一换算编码，终端精简输出仍显示 `command_deg`，完整输出额外显示 `motor_displacement_mm`。
- 2026-07-31：加入默认关闭的车辆纵向加速度前馈框架。正方向定义为固定端（零点标定端）指向电机抬升端，理想补偿 `theta_ff=-asin(a_cart/g)`，所以正加速度产生负倾角、电机端下降。串级PID将前馈叠加到反馈角后统一执行倾角及相邻变化限制；MPC把车辆加速度作为已知扰动加入整段预测，并在DOB启用时从残差中扣除该已知项。新增 `H/longitudinal_acceleration.py` 的带单调时间戳数据源协议和空数据源，当前不打开加速度串口；`--enable-acceleration-feedforward` 显式开启，无有效样本时安全使用0°，`--test-cart-acceleration-m-s2` 仅供无硬件恒值测试。完整遥测新增前馈开关、样本有效性、车辆加速度和前馈角，精简输出保持不变。
- 2026-07-31：修复中心坐标改造引入的实时启动回归：`H/ball_control_runtime.py` 在换算中心坐标目标后遗漏保存 `pipe_length_m` 局部变量，首个有效球心进入管长校验时触发 `NameError` 并退出。现于球心校验调用处直接从已验证配置读取管长，消除易遗漏的中间局部变量；启动阶段因模型/编码器初始化产生的过期首帧仍按设计丢弃，不会参与控制。
- 2026-07-31：撤销用户前一日要求的“两倍激进”远距离策略，恢复仿真验证的一倍强度：`far_drive_angle_deg` 从10°降回5°，内部误差≥5 cm时不再强制±10°，原速度增益、5°静摩擦补偿、制动和积分限幅保持不变；双向及运动中远距离测试同步恢复期望±5°。同时明确平衡保持角可随球位置变化且不必为0°：PID速度环积分可自动学习当前目标的小保持角，新增 `--equilibrium-angle-bias-deg` 允许为本次目标提供独立初值；MPC的重力预测、角度代价、回退控制和DOB均围绕该目标专属偏置计算，不假设整根管共用一个固定零偏。配置中的0°只作为省略参数时的缺省初值，完整遥测输出该值。
- 2026-07-31：进一步将比赛控制器和离线仿真的默认工作限角由±10°收紧为±5°；PID/MPC反馈、位置相关保持偏置及车辆加速度前馈叠加后的最终命令统一受该硬限制，远距离一倍策略恰好最多使用5°。串口协议能力仍保留±21.80°，只有显式传入更大的 `--working-angle-limit-deg` 才能用于专门实验，普通启动不会超过±5°。
- 2026-07-31：按临时要求仅反转串口线上电机升降位移的正负号，PID/MPC内部倾角、终端 `command_deg`、车辆加速度前馈方向、使能帧和零位帧均不改变。公共编码层新增单点开关 `H/angle_serial.py::SERIAL_SIGN_INVERTED=True`，`encode_angle()` 在 `250*tan(theta)` 后对毫米数乘以-1，闭环控制、交互电机测试及 `serial/send.py` 全部一致；完整遥测中的 `motor_displacement_mm` 显示实际线上符号。当前 `+10°` 发送 `-44.08 mm -> 92 01 2C 08 00 00 00 29`，恢复时只需将该开关改为False。
- 2026-07-31：将微信 `best(3).pt` 原样复制为 `H/weights/steel_ball_best_3.pt`，SHA-256为 `1e0ca5134ccfeb4629ff6cf5e9a9fc80221bba84daccdf8e03d4cca284438aaf`，并设为 `H/ball_depth_tracker.py` 的默认钢珠权重及GPU失败时的项目PT回退权重。原 `steel_ball_v5.pt` 和其他历史PT/engine全部保留，可继续通过 `--weights` 显式选择；权重索引、来源和校验值同步更新。
- 2026-07-31：按要求撤销临时串口正负反转，将公共开关恢复为 `H/angle_serial.py::SERIAL_SIGN_INVERTED=False`。当前正倾角重新发送正升降毫米数、负倾角发送负毫米数，例如 `+5° -> +21.87 mm -> 92 00 15 57 00 00 00 29`；PID/MPC内部角度和终端显示始终未变，使能帧及零位帧也不受影响。闭环、交互测试、独立发送工具和协议测试同步恢复正常符号。
- 2026-07-31：在控制器默认精简终端JSON中新增 `motor_mm`，与 `command_deg` 同时输出。该值不是未经量化的公式近似值，而是直接按最终8字节串口帧的符号、整数和小数字段反解得到，因此准确表示当前周期实际发送的电机升降毫米数；保持原每秒约1行频率，不增加其他调试字段。完整遥测继续使用 `motor_displacement_mm`。
- 2026-07-31：继续精简控制器默认终端JSON，移除 `valid` 和 `velocity_cm_s`，只保留 `target_cm、position_cm、error_cm、command_deg、motor_mm` 五个操作字段；失视时位置和误差仍以 `null` 明确表示，倾角/毫米数按安全归零显示。`--telemetry full` 保留有效性、速度和其他诊断字段。
- 2026-07-31：根据实机日志中5°倾角导致钢珠从目标一侧高速越过、到达另一侧边界的现象，将串级PID默认策略改为低激进版本：比赛运行与离线仿真的默认工作限角由±5°降为±2°，每个被接受视觉帧的最大倾角变化由1.5°降为0.5°；关闭误差超过5 cm时强制输出5°的远距离驱动，改为始终由位置环和速度环连续计算；静摩擦起滚补偿由5°降为1.5°，建立速度由6°/s降为2°/s。串口协议的±99.99 mm能力及±21.80°物理换算上限不变，仍可通过命令行显式放宽工作限角用于专门实验。修改后H目录86项自动测试全部通过；30组随机离线仿真中0次越过1 cm边界、8组在20秒内稳定到位，最坏错误侧越过0.6521 cm。8个严苛联合端点场景仍有4次越界且没有场景在20秒内达到连续稳定，说明该版本降低了常规超调但响应明显变慢，只作为实机低风险起点，仍需依据实际摩擦和保持角继续调参。
- 2026-07-31：针对钢珠受到一次位置扰动后持续在目标两侧大幅震荡的问题，进一步降低串级PID大偏差恢复强度：位置环比例由1.2降为0.6 s^-1，最大目标速度由3.5降为1.2 cm/s，速度环积分由12降为4 deg/m，静摩擦起滚补偿由1.5°降为0.8°且建立速度由2降为1°/s，每个有效视觉帧最大倾角变化由0.5°降为0.25°；速度环比例仍为25 deg/(m/s)，默认工作限角仍保留±2°作为高速时的反向制动能力。测试新增“实际速度超过参考速度时必须反向制动”的默认参数断言，H目录86项自动测试全部通过。30组随机不确定性仿真中3次越过1 cm、4组在20秒内稳定到位；8个严苛场景仍4次越界且0组稳定，说明该组参数抑制驱动强度的同时可能在高摩擦场景推不动或收敛过慢，当前按实机调试要求作为待验证默认值，不视为最终参数。
- 2026-07-31：为 `H/start_ball_controller.sh` 增加默认打开的串级PID实时调参UI。新增 `H/control_tuning_ui.py`，以独立Tk进程显示全部20项可调值：17项 `cascade_pid` 参数、工作倾角上限、每个视觉帧最大角度变化和平衡保持角初值；界面消息由25 Hz监督循环非阻塞轮询，窗口刷新不进入视觉或50 Hz串口线程。点击应用会校验有限值和安全范围，清除旧PID积分及接近状态，从当前已限幅倾角继续平滑控制；MPC不误用PID界面，`--no-control-ui` 可关闭窗口。
- 2026-07-31：新增 `H/control_profiles.py` 和 `H/control_profiles` 参数方案目录。UI支持把当前参数另存为命名JSON、选择并应用已有方案、重命名方案和恢复启动输入；方案名拒绝路径分隔符，文件采用临时文件替换方式原子保存。最后一次保存并应用或选择并应用的文件名写入 `.active_profile`，下次启动在基础配置上自动加载该方案；重命名当前默认方案时同步更新指向。建立 `low_oscillation_default.json` 作为初始默认方案，命令行工作限角仍可临时覆盖。新增参数完整性、越界拒绝、目录逃逸拒绝、保存/加载/默认选择/重命名及实时应用清积分测试。
- 2026-07-31：将微信 `best(4).pt` 原样复制为 `H/weights/steel_ball_best_4.pt`，源文件和项目副本SHA-256均为 `75d6dabe3205a9c6dac4d24fc15c46886a3a28093bf8653a6f16fb00ab420aff`，并设为 `H/ball_depth_tracker.py` 的默认钢珠权重及GPU失败后的项目PT CPU回退权重。此前 `steel_ball_best_3.pt`、v5及其他历史PT/engine全部保留，可继续通过 `--weights` 显式选择；权重索引和运行文档同步更新。
- 2026-07-31：重做串级PID实时调参窗口外观与输入方式：默认窗口由940×700扩大为1220×820、最小尺寸提高为980×650，统一放大字体、输入框箭头和按钮，增加蓝色标题区、白色参数卡片、状态栏及“安全限制与执行器、位置环与制动规划、速度环、起滚与远距离策略、死区与积分限幅”五组分区；参数区支持滚轮与滚动条。每个参数同时提供横向拖动条、可直接输入的Spinbox及上下箭头，拖动值按该参数步长量化，输入/箭头/方案加载/恢复启动值均与拖动条双向同步；原有实时应用、另存参数文件、选择默认方案和重命名功能保持不变。
- 2026-07-31：修复新版PID调参UI大量方框乱码：系统字体检查确认已安装 `Noto Sans CJK SC`，界面不再把Tk命名字体 `TkDefaultFont` 错当作实际字体族，而是把Tk默认、文本、菜单、标题以及所有自定义标题/按钮样式统一显式绑定到 `Noto Sans CJK SC`。同时把单位中的度符号和上标2分别改为兼容文本 `deg`、`^2`，范围分隔符改为“到”，降低特殊字符缺字风险；参数含义和数值不变。
- 2026-07-31：将 `cascade_pid.position_deadband_m` 默认值由0.002改为0.003 m，并明确为可由UI及参数方案保存的“电机保持死区”。请求目标误差不超过0.3 cm且滤波球速不超过 `velocity_deadband_m_s=0.008 m/s` 时，串级PID冻结位置/速度积分和起滚补偿，保持上一倾角指令不动，避免毫米级视觉抖动驱动电机；若球仍高速经过死区则继续执行速度环制动，显式非零平衡角或车辆加速度前馈也不被位置死区屏蔽。默认参数文件同步为0.003 m，并新增低速保持、积分冻结及高速仍制动测试。
- 2026-07-31：针对管道各位置局部坡度变化，加入纯运行期的临时局部角度零点学习，不修改钢珠位置坐标零点，也不建立或保存位置—角度映射。目标死区外若下发倾角绝对值至少0.2°、滤波速度不超过0.003 m/s且连续2秒内位置变化不超过0.0015 m，则把当前下发倾角吸收到该处临时角度零点、清除旧速度积分，并在此基础上继续叠加PID增量；临时零点限制在±1.2°且最终命令仍受±2°默认工作限角。钢珠离学习点超过0.015 m、失视重置、参数重新应用或程序退出即清除，下一处重新判断。按用户要求，全部判定常量固定在 `ball_control.py`，不加入UI、JSON配置或参数文件；终端在刷新时输出一次提示，完整遥测增加临时零点角和位置。新增停滞学习及离开管段自动遗忘测试。
- 2026-07-31：修复临时局部角度零点跨过1.5 cm后硬清零引起的控制阶跃。实测在中点学习到约-0.8°后，钢珠刚开始向正方向运动便因越过距离阈值丢失-0.8°偏置，等效指令瞬间增加约+0.8°并造成回弹。现撤销运动中的距离清零：控制器只携带一个运行期临时偏置，不保存位置表；钢珠运动到下一段时继续沿用，若再次停滞则由当时下发倾角覆盖旧偏置。只有视觉失效安全归零、控制器重置、重新应用参数或程序退出时才清除。测试改为验证跨段运动偏置连续、显式重置才清零。
- 2026-07-31：修复临时局部零点已在+4.156 cm刷新为-0.576°后，误差仍有约0.85 cm但指令长期停在-0.58°的问题。根因不是零点未刷新，而是两阶段控制从下方接近5 cm目标时先使用4.4 cm安全预停点；球在约4.15 cm时距预停点仅0.25 cm，旧代码复用最终0.3 cm电机保持死区把内部预停误差清为0，形成“局部零点-0.576° + PID增量0”的死锁，同时请求误差0.85 cm又未达到0.6 cm二阶段解锁条件。现移除对内部预停误差的死区归零，只在最终请求目标误差≤0.3 cm且低速时保持电机；因此预停点前剩余0.25 cm仍会在临时零点基础上产生PID增量并继续推进。新增该实测数值组合的回归测试。
- 2026-07-31：上述实测组合的回归测试进一步发现 `braking_margin_m=0.003` 的旧制动公式 `max(|error|-margin,0)` 也会在距4.4 cm预停点不足0.3 cm时把目标速度硬置0，即使移除内部死区仍可能停死。将制动距离改为连续软包络 `d_eff=d²/(d+margin)`：margin增大仍会更早压低允许速度，但只有实际控制误差d为0时速度上限才归零；结合最终目标死区和两阶段预停，不再增加一个无法穿过的提前停止带。
- 2026-07-31：串级PID实时调参UI新增独立“运行目标点”控制区，按当前管中心坐标定义提供 `-12.0～+12.0 cm` 滑条、两位小数输入框和上下箭头，点击“应用目标点”后由25 Hz监督循环实时更新闭环目标，无需重启视觉或串口。目标切换会新建比赛目标监视器、清除旧目标的积分及两阶段接近状态，并让角度限速器从当前倾角继续，避免指令突跳；旧目标的稳定/越界锁存同时清除。目标属于运行指令，不写入PID参数文件，下次启动初值仍由 `--target-cm` 指定。
- 2026-07-31：将微信 `v6_server_real_env.pt` 原样复制为 `H/weights/steel_ball_v6_server_real_env.pt`，源文件和项目副本SHA-256均为 `37ab4b267984a2b3974b4fd9596ad92d375a4a2baefa4e1d568a7eca8f902ad0`，并设为 `H/ball_depth_tracker.py` 默认钢珠权重及GPU加载失败后的项目PT CPU回退权重。此前 `steel_ball_best_4.pt` 及所有其他历史PT/engine均保留，可继续通过 `--weights` 显式选择；权重索引和运行文档同步更新。
- 2026-07-31：修复两阶段接近在约0.7～0.9 cm最终误差处指令极小、长时间停滞的问题，不提高全局PID增益。静摩擦补偿原先只看相对0.6 cm安全预停点的内部误差，球距预停点仅0.12 cm时会因小于0.6 cm起滚门槛而关闭；现只要仍在两阶段接近且未进入最终0.3 cm电机保持死区，就允许原0.8°上限、1°/s斜率的补偿缓慢建立。二阶段解除条件在原0.6 cm最终误差阈值上增加0.15 cm视觉噪声余量，最终误差≤0.75 cm且低速持续0.6秒即可开始缩小预停偏置。新增目标-5 cm、位置约-4.28 cm等价工况测试，验证补偿启动和二阶段解锁；默认±2°工作限角、每视觉帧0.25°变化限制及PID四项增益均保持不变。
- 2026-07-31：将临时局部角度零点的停滞更新时间由代码固定常量改为串级PID参数 `local_zero_stall_time_s`。基础配置与 `control_profiles/low_oscillation_default.json` 均默认2.0秒，旧参数方案缺项时由校验层自动补为2.0秒；参数可随方案加载并由调参UI实时设置（0.1～10.0秒）。位置模式和速度模式共用该值，临时零点数值本身仍只存在于本次运行、不写入参数文件。新增短更新时间生效和旧方案兼容回归测试并同步操作文档。
- 2026-07-31：修复速度模式中临时局部零点与静摩擦补偿重复叠加导致指令固定在 `-1.2°-0.85°=-2.05°` 的问题。刷新局部零点时现按清除速度积分后的P/静摩擦动态驱动量进行无扰分解，只把剩余保持偏置转入局部零点；刷新后静摩擦补偿仍正常叠加，但不会被局部零点重复吸收，刷新前后总倾角连续。另增加持续停滞自适应增驱：判定停滞成熟后按 `stall_drive_boost_ramp_deg_s`（默认0.25 deg/s）缓慢增加，最大 `stall_drive_boost_max_deg`（默认1.0°），可吸收部分周期性无扰转入±1.2°局部零点，超出部分继续保留；球恢复运动时增驱平缓退回0°，总命令仍受工作限角。两项参数已加入基础配置、`low_oscillation_default.json`、当前 `myexample.json`、实时UI及旧方案兼容默认值，完整遥测新增 `stall_drive_boost_deg`。新增实测参数等价工况回归测试，验证刷新后约-1.35°仅平滑变为约-1.36°而不会跳至约-2.20°。
- 2026-07-31：为位置控制增加目标点局部零点先验，同时保留原停滞阈值实测覆盖。基础配置新增 `position_local_zero_prior`：用户实测 `-5 cm -> 电机+1.0 mm`、`+5 cm -> 电机-1.5 mm`，目标在两点间按位置线性插值，区间外夹紧最近端点而不外推，再用 `theta=atan(mm/250)` 转成内部倾角（分别约+0.229°、-0.344°，中心约-0.057°）。位置模式启动、UI切换目标点、参数重新应用或视觉恢复重置时先写入该预测偏置；它不增加输出硬限幅，也不替代 `local_zero_stall_time_s`，钢珠持续停滞达到阈值后仍由实际控制响应无扰更新。速度调试模式没有目标位置，因此不使用该位置先验。新增端点、中心插值和区间外夹紧测试，并在终端显示本次目标采用的毫米/角度先验。
- 2026-08-01：根据新一轮实测，将位置局部零点先验的 `-5 cm` 标定值由电机 `+1.00 mm` 修正为 `+0.65 mm`，`+5 cm -> -1.50 mm` 保持不变。两点间仍线性插值，因此中心0 cm先验由-0.25 mm更新为-0.425 mm；区间外仍夹紧最近端点，位置模式停滞达到 `local_zero_stall_time_s` 后仍由实际学习覆盖，不把先验当作最终固定值。同步更新配置、操作文档及端点/中心/夹紧回归测试。
- 2026-07-31：默认精简控制终端恢复速度信息，并同时输出 `target_velocity_cm_s` 与 `velocity_cm_s`。前者是串级PID位置环计算后交给速度环的目标速度，后者是视觉位置序列经状态估计得到的实际速度，二者均使用“朝电机端为正、朝原固定端为负”的cm/s单位；可直接识别实际速度超过目标速度时速度环是否正在反向制动。失去有效视觉或MPC没有独立目标速度时相应字段显示 `null`，原目标、位置、误差、倾角和电机毫米字段保持不变。
- 2026-07-31：为减少终端每行长度，将默认compact JSON的七个操作字段统一缩写为 `tgt、pos、err、v_tgt、vel、deg、mm`，分别表示目标位置(cm)、实际位置(cm)、位置误差(cm)、位置环目标速度(cm/s)、视觉估计实际速度(cm/s)、倾角命令(deg)和电机升降位移(mm)。仅默认精简显示改名，`--telemetry full` 的完整诊断字段名称保持不变。
- 2026-07-31：新增串级PID `velocity` 独立速度环调试模式。命令 `--control-mode velocity` 绕过位置环、两阶段预停点和位置死区，不定义目标位置；初始目标速度由 `--target-speed-cm-s` 指定，UI顶部实时设定范围为±5 cm/s。速度模式直接计算 `e_v=v_ref-v`，继续使用速度Kp/Ki、静摩擦起滚补偿、平衡/加速度前馈、±2°工作限角和每视觉帧0.25°变化限制；实际速度超过目标时禁止起滚补偿覆盖速度PI的反向制动。按用户补充要求，速度模式仍利用球的位置保留临时局部角度零点学习：目标速度非零但球连续2秒基本不动时吸收当前倾角，运动中携带并在下一停滞管段覆盖，避免不同位置坡度被错误归因于速度环。速度模式compact仅输出 `pos、v_tgt、vel、deg、mm`，不显示目标位置/位置误差；普通位置模式的 `--target-cm` 改为可省略且默认0 cm。新增驱动、超速反向制动、局部零点学习、无目标位置遥测和默认参数解析测试。
- 2026-07-31：新增 `H/control_curve_ui.py` 独立实时曲线进程，默认随串级PID控制器另外打开一个类似VOFA的深色网格窗口。位置模式提供“速度：目标/实际”和“位置：目标/实际”两个标签页；速度模式只创建速度标签页，不生成目标位置曲线。横轴采用每个被接受视觉样本的采集单调时间、滚动显示最近30秒，纵轴分别以cm/s和cm自动缩放，目标/实际使用两种颜色并显示最新数值。父控制进程通过容量64的非阻塞队列投递，队列满时丢最旧绘图点，Tk窗口以10 Hz刷新，因此绘图/X11卡顿不会阻塞15～20 FPS控制更新或50 Hz串口线程；`--no-plot-ui` 可关闭。加入纵轴范围与空数据测试。
- 2026-07-31：为速度环实机调试加入方向相关端点锁存与UI重启状态机。由于钢珠半径0.5 cm使球心理论可达坐标约为±12.0 cm，固定在可达端点内再留0.5 cm检测余量，即球心≥+11.5 cm且目标速度为正、或球心≤-11.5 cm且目标速度为负时触发；反向离开端点不触发。触发后暂停速度PI更新并持续发送触发前最后倾角，即使随后视觉超时仍等待人工处理。速度模式初始为0°暂停，调参UI新增“倾斜角返回0”和“启动速度环”：返回0立即解除边缘锁存、暂停跟踪并发送0°；启动按钮清除旧估计/速度积分和缓存视觉记录，等待下一条有效钢珠测量后才恢复目标速度跟踪。返回0、重新启动及修改目标速度均保留运行期临时局部角度零点，避免管段坡度重新混入速度环。新增±11.5 cm双向触发、反向不触发及阈值换算测试。
- 2026-07-31：针对视觉位置微抖经求导后造成的钢珠速度尖峰，在现有常加速度Kalman估计之后新增 `VelocityLowPassFilter`。滤波器按相邻有效视觉样本的真实单调时间戳使用精确一阶系数 `alpha=1-exp(-dt/tau)`，默认 `tau=0.12 s`，而不是假定固定帧率；配置项为 `velocity_filter_time_constant_s`。位置输出不经过该层，滤波后的速度统一供串级PID/MPC反馈、端点/稳定判定、compact `vel`、完整遥测、调参诊断和VOFA式曲线使用，避免各处观察值与控制值不一致。速度环重新启动、估计器持续失视重置时同步清空滤波状态。新增交替±0.1 m/s噪声衰减、恒速收敛及reset测试。
- 2026-07-31：将 `velocity_filter_time_constant_s` 加入实时调参UI“视觉速度滤波”分区和PID参数方案，范围0.02～1.00 s、步长0.01 s；应用后立即改变同一低通实例的时间常数，无需重启或切换速度数据源，保存/加载参数方案时一并保留。默认方案补入0.12 s，参数完整性与实时应用测试覆盖该值。
- 2026-07-31：根据静止球实测位置约在-5.353～-5.393 cm间抖动、低通速度仍在约±0.09 cm/s摆动的日志，为速度滤波增加静止感知钳零，而非继续单纯增大时间常数。默认连续0.4 s有效视觉窗口内球心位置极差≤0.0008 m（0.08 cm），且一阶低通后速度绝对值≤0.001 m/s（0.10 cm/s）时，将公开速度及滤波内部残余同时置0；明显运动会因位置跨度或速度超限而保留正常输出。配置新增 `velocity_stationary_window_s=0.4`、`velocity_stationary_position_span_m=0.0008`、`velocity_stationary_threshold_m_s=0.001`，控制、终端和曲线继续共用同一结果。测试覆盖交替假速度在静止小范围位置抖动下归零，以及0.5 cm/s真实匀速不被钳零。


./H/start_ball_controller.sh \
  --special-task minus4p5_then_plus5 \
  --enable-serial \
  --no-stream

./H/start_ball_controller.sh \
  --controller cascade_pid \
  --target-cm 5 \
  --enable-serial \
  --no-stream

./H/start_ball_controller.sh \
  --control-mode velocity \
  --target-speed-cm-s 1.0 \
  --enable-serial \
  --no-stream

./H/start_ball_zero_calibration.sh

sudo systemctl start nuedc-h-hotspot.service
sudo systemctl start nuedc-h-competition.service

sudo systemctl stop nuedc-h-competition.service
sudo systemctl stop nuedc-h-hotspot.service

./H/competition_status.sh --watch
journalctl -u nuedc-h-competition.service -f